spring:
  application:
    name: orderservice  # nacos data id 前缀
  profiles:
    active: dev  #集成环境，nacos data id 后缀
  cloud:
    nacos:
      server-addr: 120.24.215.98:8848  #Nacos地址
      config:
        file-extension: yaml  #文件后缀名
